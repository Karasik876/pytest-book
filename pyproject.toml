[project]
name = "pytest-book"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cards",
]

[tool.uv.sources]
pytest = { workspace = true }
cards = { path = "cards_proj" }

[dependency-groups]
dev = [
]
